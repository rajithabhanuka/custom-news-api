spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/comppress?allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    username: covidtesting.noreply@gmail.com
    password: covidtesting@2020
    port: 25
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
server:
  port: 8080
  servlet:
    context-path: /news-api
scheduler:
  enabled: false
  triggeringIntervalMilliSeconds: 20000
  initialDelayIntervalMilliSeconds: 5000
